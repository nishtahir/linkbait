image: java:8-jdk
before_script:
    - chmod +x gradlew

build:
  stage: build
  script:
    - ./gradlew -g /cache build
  allow_failure: false

test:
  stage: test
  script:
    - ./gradlew -g /cache check
